# GENERATED BY PUPPET using /modules/xen/templates/vmconfig.cfg 

#
# Assegnazione cpu
#
cpus='<%= cpus %>'
vcpus='<%= vcpus %>'

#
#  Kernel + memory size
#
kernel      = '<%= kernel %>'
ramdisk     = '<%= ramdisk %>'
memory      = '<%= memory %>'
extra       = '<%= extra %>'

#
#  Disk device(s).
#
root        = '<%= root %>'
disk        = [ <% disks.each do |disk| -%>'<%= disk -%>',<% end -%> ]

#
#  Hostname
#
name        = '<%= name %>'

#
#  Networking
#
vif         = [ <% vifs.each do |vif| -%>'<%= vif -%>',<% end -%> ]

#
#  Behaviour
#
on_poweroff = '<%= on_poweroff %>'
on_reboot   = '<%= on_reboot %>'
on_crash    = '<%= on_crash %>'

